# 리팩토링 로드맵 — 스튜디오 로컬 우선 + 하이브리드 (2026-07-21 · C안 확정 2026-07-22)

> 요청: 4계층 블루프린트 관점에서 스튜디오 도입 시 필요한 하드웨어·관제 시스템·Orca 도입을 검토하고 시스템을 리팩토링.
> 서비스(stage.ai.kr / impact ledger / papercraft.kr)는 **보존**, 삭제 대상은 **에이전트 잡동사니만**.
> 목표: Claude Code 작업이 24시간 로컬(온·오프라인) + 어려운 것만 온라인 + 어떤 디바이스든 온라인 관제 + 자동화(수집→포스팅 / 주제→퀴즈 / 주제→영상→포스팅) 고도화.
> 이 문서는 병렬 검증(WebSearch/WebFetch, 실제 소스 확인) 결과를 종합한 것이며, **미확인 항목은 [확인필요]로 표기**한다.

---

## 0. 핵심 판정 — "처음부터 리팩토링"은 부분적으로 과잉이다 (보수적)

정직하게: **서비스는 리팩토링 대상이 아니다. 에이전트 배관만 리팩토링 대상이다.**

- **papercraft.kr = 이 레포(actioncraft-web)**, 정상 동작하는 Next.js 16 + Supabase 서비스. 리팩토링 불필요 — **보존**.
- **이 레포에는 삭제할 '에이전트 잡동사니'가 없다.** 삭제 대상인 celery + `agents/bot.py` + 채널별 봇(`!regen !cos !god !fast`)은 **별도 레거시 레포**에 있다(이 레포 아님). [확인필요: 레거시 레포 경로]
- **이미 어려운 리팩토링의 절반은 끝났다.** 뒤엉킨 멀티봇 → 경량 관제 레이어(hooks·git-sync·2채널)로의 전환이 그것이고, heartbeat가 실제 작동 중이다.

→ 그래서 "from scratch 재구축"이 아니라 **① 레거시 에이전트 폐기 ② 신규 로컬-우선 에이전트 레이어 완성(진행 중) ③ 자동화를 신 구조로 이전**의 3단계다. **최소 침습 원칙**: 서비스는 건드리지 않고, 신규 레이어를 덧붙여 검증한 뒤, **레거시는 맨 마지막에만** 삭제한다(대체 검증 전 삭제 금지).

---

## 1. 보존/삭제 경계 (절대 규칙)

| 구분 | 대상 | 조치 |
|---|---|---|
| **보존(건드리지 말 것)** | `src/`, `supabase/`, `data/`, `public/`, 루트 빌드·설정(`next.config.ts`, `package*.json`, `tsconfig.json`, `eslint.config.mjs`, `postcss.config.mjs`), 환경변수(Supabase 키, NEXTAUTH_*, NEXT_PUBLIC_SITE_URL) | papercraft.kr 서비스 코드 |
| **보존·발전** | `agent-config-template/`, `docs/` | 신규 경량 관제 레이어 (삭제 대상 아님) |
| **삭제(맨 마지막)** | 레거시 레포의 celery / `agents/bot.py` / 채널별 슬랙봇 | 신 구조 2주 검증 후에만 |
| **[확인필요]** | stage.ai.kr, impact ledger | 실체·레포·URL 미확인 → **확인 전 어떤 정리에서도 제외** |

> papercraft.kr 실체(확인됨): Paper Engineering Studio(PE Studio), 대표 오세기, 2013 창업, 자기구조설계 특허 11종, 현대백화점·KAIST 등 650건+ 납품. `ask@papercraft.kr`. (출처: `src/lib/site.ts`)

---

## 2. 타깃 아키텍처 (4계층 × 로컬-우선 하이브리드)

```
[관제/인터페이스]  Slack #agent(명령)·#agent-log(관제)  +  (선택) Orca 폰앱  +  Tailscale SSH/Remote Control
        │  어떤 디바이스든 온라인 관제
[오케스트레이션 + 지능·로컬]  Mac Studio 96GB (24시간 상시)
   Claude Code(headless, launchd) + hooks/heartbeat + 라우터(.ccr): 쉬운/오프라인→로컬, 어려운→온라인 Claude
   MLX: Qwen3 30~35B MoE(70~87 tok/s) + 로컬 이미지(Draw Things/MLX-Flux)
        │                              [지능·온라인]  Claude API (어려운 5~15%만)
[제어 HOOKS]  heartbeat·guard·git-sync (결정론적, LLM 무관) → 실패는 #agent-log로 push
[무거운 보조]  NAS DXP4800+ Docker(리눅스 네이티브): n8n·스크래핑·큐 + 스튜디오 감시 워치독 컨테이너
[저장]  git 레포(설정·스킬 = 단일 진실원)  +  NAS(모델 가중치·생성물·상태·백업)
[실행 SUBAGENTS/자동화]  수집→포스팅 / 주제→퀴즈 / 주제→영상  (사업·용도별 격리)
```

**4계층 매핑**: CLAUDE.md(상시 맥락) · Skills(스킬, `_quarantine` 심사) · MCP(커넥터 최소화) · **Hooks(결정론적 제어 — 이미 구축)** · Subagents(자동화 파이프라인) · Plugins(검증된 구성 패키지화).

---

## 3. "24시간 로컬 + 어려운 것만 온라인 + 어디서든 관제" — 실제 가능 범위 (정직하게)

문자 그대로 되는 것과 타협이 필요한 것을 구분한다:

- ✅ **24시간 로컬 오케스트레이션**: Claude Code headless를 launchd로 스튜디오에 상시 구동. 됨.
- ⚠️ **"오프라인에서도 Claude Code"**: Claude(클라우드)는 오프라인 불가. 정확히는 **"오프라인일 땐 로컬 모델로, 온라인이고 어려우면 Claude로"** 전환하는 하이브리드가 가능하다. 라우터(.ccr / OpenAI 호환 로컬 엔드포인트)가 이 전환을 담당. → "오프라인에도 도는 로컬 에이전트 + 온라인 시 Claude 승급"으로 이해하면 100% 달성 가능.
- ✅ **어려운 것만 온라인**: 라우터 규칙(신뢰도·난이도·대외 산출물) + `routing-policy.md`. 됨.
- ✅ **어디서든 관제**: Slack 2채널(모바일 포함) + (선택) Orca 폰앱(완료 알림·폰에서 지시) + Tailscale SSH. 됨.
- ✅ **스케줄링**: Claude Code `CronCreate`/`/loop` + Cloud Scheduled Tasks(로컬 꺼져도 실행) + launchd. 됨.

---

## 4. Orca IDE — 조건부 도입 (보완재, 코어 아님)

- **정체**: Claude Code·Codex·Cursor CLI 등 30+ CLI 에이전트를 격리 git worktree에서 병렬 실행하는 오픈소스 오케스트레이터(ADE). Stably AI(YC), **MIT 무료 + BYO-구독**(자체 마크업 없음). macOS Apple Silicon 앱 + **iOS/Android 폰앱(모니터링·지시·완료 알림)** + VPS.
- **맞는 부분**: "어디서든 온라인 관제" → 폰앱이 직접 부합. 병렬 개발 세션 관리에 강함. 스튜디오에 설치 가능.
- **안 맞는 부분**: (1) **24시간 무인 스케줄링(cron) 미구현**(로드맵 이슈 #5708 단계) (2) **오프라인 로컬모델 공식 지원 [불확실]**. → **무인 자동화 백본으로 쓰면 안 됨.**
- **판정**: **대화형 병렬 개발 + 폰 원격 관제 레이어로만 제한 도입.** 방금 멀티봇 복잡도를 걷어낸 흐름에서 GUI 앱을 무인 서버 자동화에 끼우면 복잡도가 되돌아온다. 자동화 스케줄 백본은 Claude Code + cron/hooks로 유지.

---

## 5. 자동화 3종 고도화 설계 (로컬/클라우드 경계)

공통: Claude Code headless 오케스트레이터 + 로컬 모델(요약·구조화 상시) + 어려운 스텝만 Claude + hooks→Slack 관제.

| 자동화 | 로컬 가능? | 설계 |
|---|---|---|
| (a) 수집→포스팅 | ✅ 100% 로컬(오프라인) | RSS/스크래핑 → 로컬 LLM 요약·초안 → 검토큐 → 포스팅. 노드 많으면 n8n 자가호스팅(Docker) 고려 |
| (b) 주제→퀴즈 | ✅ 100% 로컬 | 로컬 LLM structured output(JSON 스키마 강제) + **검증·재시도 가드**(로컬 구조화 ~6% malformed) |
| (c) 주제→영상→포스팅 | ⚠️ 렌더만 클라우드 | 스크립트·TTS·이미지·자막·썸네일·업로드 = 로컬 / **영상 렌더 1스텝만 클라우드 API**(Kling 3.0 ≈$0.35–0.5/5s, 오디오·립싱크 필요 시 Veo 3.1 Fast ≈$0.75/5s). Mac 로컬 영상(Wan 2.2)은 클립당 수십 분 → 24h 생산라인 부적합, 실험·백업 렌더용만 |

> n8n vs 순수 스크립트: 경량 철학 유지를 위해 **노드 많은 (a)/(c)만 n8n, 단순한 (b)는 스크립트**로 절충 권장.

---

## 6. 하드웨어 최종 판정 — **C안 확정(2026-07-22): 스튜디오 24h + NAS 도커, 미니 정리**

추가 구매는 **NAS 램 증설 하나뿐**(금요일 예정). 역할 재배치가 핵심:

- **Mac Studio 96GB (24시간 상시)** = 두뇌+오케스트레이션 통합: 로컬 LLM 서빙(MLX; Qwen3 30~35B MoE 70~87 tok/s) + 로컬 이미지 + Claude Code headless + hooks/heartbeat + 라우터. 오케스트레이션 부하는 2~6GB라 96GB에 얹어도 여유.
- **NAS UGREEN DXP4800 Plus** = 무거운 보조 + 저장 + 보조 워치독. UGOS Pro의 Docker 공식 지원(x86 리눅스 네이티브 — macOS 도커의 VM 오버헤드 없음)으로 n8n·스크래핑·큐 구동 + 스튜디오 heartbeat 감시 컨테이너(→ B안의 '관제 동반 사망' 약점 상쇄). **램 8→32GB 증설 후** 무거운 컨테이너 투입. Pentium Gold 8505는 자동화엔 충분하나 **LLM 추론 금지**. 접근은 **Tailscale 전용, 외부 노출(포트포워딩) 금지**.
- **Mac Mini 24GB** = 스튜디오 관제 2주 무사고 후 정리(처분/타용도). 그 전까진 병행 유지 — 대체 검증 전 삭제 금지.
- **온라인 분기** = 어려운 추론 → Claude, **영상 렌더 → 클라우드 API**(로컬 불가한 유일한 항목).

**Mac RAM 256/512GB 업그레이드 = 권장 안 함.** "어려운 추론은 온라인 Claude" 전략과 중복 투자다. **소형 UPS(600~1000VA)** 1대로 정전 시 안전 종료만 확보. 예외: 영상이 '매일 다수편 뽑는 핵심 프로덕트'가 되면 그때만 CUDA 박스/상시 클라우드 GPU 검토.

- C안 근거: ① 추론 크리티컬 패스에서 기기 간 연결 제거(#1 장애 유형이 비핵심 경로로 이동 — NAS 링크가 끊겨도 자동화만 멈추고 코어 무사) ② NAS 워치독으로 감시자 분리 확보 ③ 유지 기기 -1. (DXP4800+ 검증: Pentium Gold 8505 / DDR5 최대 64GB / 10GbE / UGOS Docker 공식 지원)

- 대역폭 819GB/s가 로컬 토큰속도를 좌우 / Apple Silicon 약점 = 긴 프롬프트 prefill(MLX가 llama.cpp 대비 4~5배 개선) / 영상 로컬은 Metal 지원 미흡으로 비실용(2026 커뮤니티 합의).

---

## 7. 턴별 빌딩 로드맵 (순차, 각 턴 = 빌드→검증→다음)

1. **T1 · 96GB 검증** — ✅ **완료(2026-07-22)**: `qwen3.6:35b-a3b` @ 스튜디오 실측 **83.0 tok/s**(예측 70~87 적중), 도구호출 3/3 · JSON 3/3 · 과잉호출억제 PASS · 일관성 3/3 → **모델 확정, 96GB 충분**. 단 Q1(대외 견적)에서 **가격 산술 10× 오류(300×7,500원=225만원을 2,250만원으로) + 미확인 사실 날조(선금 50%·가공방식 등)** 확인 → **대외 산출물은 반드시 온라인 Claude 경유** 라우팅 원칙이 실측으로 재확인됨.
2. **T2 · 스튜디오 상설화(관제 이전)** — 초기화된 스튜디오 셋업(전원·자동 로그인·Tailscale·Claude Code) → 레포 clone → `agent-config-template/install.sh`로 관제 이식(미니 관제는 병행 유지) → MLX 서빙 상설(**Tailscale 주소 바인딩**, `.local` 금지) + `.ccr` 라우터. *(§연결성: agent-architecture.md §15)*
3. **T3 · 24시간 오케스트레이터 @ 스튜디오** — Claude Code headless(launchd) + Cron/Scheduled + Slack 관제 확립.
4. **T3.5 · NAS 투입(금요일 램 32GB 증설 후)** — Tailscale 설치 → Docker(n8n) → 스튜디오 heartbeat 감시 워치독 컨테이너. 외부 노출 금지.
5. **T4 · 자동화(a) 수집→포스팅** — 첫 파이프라인(가장 단순·고가치), n8n@NAS + hooks→#agent-log 관제.
6. **T5 · 자동화(b) 퀴즈** — 100% 로컬, 스키마 검증 가드.
7. **T6 · 자동화(c) 영상** — 로컬 오케스트레이션 + 클라우드 렌더 API. 가장 복잡 → 마지막.
8. **T7 · 레거시·미니 정리** — 신 구조(스튜디오 관제) 2주 무사고 후 레거시 에이전트 삭제 + 미니 정리. **stage.ai.kr·impact ledger 경계 확인 후에만.**
9. **T8 · 사업 B·C 온보딩** — biz-b/c 정의 확정 후 스킬 네임스페이스·채널 추가.

> 원칙: **대체가 검증되기 전엔 아무것도 삭제하지 않는다.** 각 자동화는 켜고 끌 수 있게(개별 kill switch), 실패는 5분 내 #agent-log로.

---

## 8. 진행 전 [확인필요] — 사용자 확인 대기

보수적 검토상, 아래가 확인돼야 T7(삭제)·T8(사업 B/C)이 안전하다:

1. **레거시 에이전트 레포/경로** — celery·`agents/bot.py`·채널봇이 있는 곳(삭제는 여기서, 이 레포 아님).
2. **stage.ai.kr** — 무엇이고 레포·배포 위치는? (프록시 403으로 실체 미확인)
3. **impact ledger** — 정식 URL·레포? (검색으로 특정 불가)
4. **사업 B·C 이름·핵심 업무** — CLAUDE.md placeholder 확정용.

이 4가지가 없으면 서비스·삭제 관련 작업은 **보류**하고, T1~T6(신규 레이어·자동화)만 먼저 진행한다.

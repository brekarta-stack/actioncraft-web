# 설계 통합 대조표 (내 세션 산출물 × 사용자 아키텍처 문서 5종) — 2026-07-22

> 대상: 사용자 문서(CLAUDE.md · agent-orchestration-architecture.md · routing-design.md · design-maturity-checklist.md · architecture-knowledge-map.md)
> × 세션 산출물(system-architecture-v3 · channel-reorg-proposal · current-system-domains · product-portfolio-management · 14-day-plan).
> 목적: ① 일치/상충/빈틈 정리(본 문서) → ② 상충 판정 → ③ 게이팅 3결정 확정 → ④ 통합 설계도 + 루트 CLAUDE.md 갱신.
> 불변 원칙 유지: 멀티모델(탈클로드) · 운영 API/로컬 무중단 · LLM 직접 주문권한 금지 · 2주·초보 제약 · 과잉설계 금지.

## 1. 일치점 (양쪽 수렴 — 교차검증 완료, 이견 없음)
- n8n(NAS) 결정적 백본 + LLM은 "한 스텝"으로만. 채널별 상시 챗 에이전트 폐기.
- LiteLLM 단일 관문 + 역할 별칭 + 예산캡(80%경고/100%강등) + 폴백체인. 무중단 = 자동 강등.
- 운영을 Claude Code 구독토큰에서 분리(API/로컬). Claude Code는 개발도구로 강등.
- 매매: LLM 분석가(읽기전용, 주문권한 0) ↔ 결정적 집행엔진 격리. KIS OpenAPI(국내+미국, 모의→실전), 토큰 세션앵커 갱신, 레이트리밋 토큰버킷, 웹소켓 재접속.
- 관제 3종(n8n Insights + LiteLLM UI + **Uptime Kuma@mini**) + Slack #ops. 자체 대시보드 재제작 금지.
- Slack Socket Mode(인바운드 0) 4채널, 포트포워딩 금지, Tailscale 사설망.
- 백업: pg_dump + N8N_ENCRYPTION_KEY 별도보관 + 복원 리허설, 이미지 버전핀.
- 교살자 이주 + "켜기 전에 끈다"(중복 방지). LinkedIn/YouTube 0일차 신청. 블로그=WordPress(네이버 반자동).
- 한국어=Qwen/A.X(Apache2.0), EXAONE=개인학습 전용(비상업). 매매 시그널 판매 불법.

## 2. 상충점 (판정 필요 — §다음 단계)

| # | 항목 | 사용자 문서 | 내 세션 | 1차 판정 방향(근거는 2단계) |
|---|---|---|---|---|
| C1 | **매매 엔진 위치** | mini(장중 웹소켓 근접, 24/7) — 단 CLAUDE.md §D는 "게이팅 미결"로도 표기 | 결정 보류였음 | → **NAS 컨테이너** 권고(비HFT라 지연 무의미, NAS가 진짜 24/7 티어, 관제·백업 일관). 페일세이프 정지 조건 부가 |
| C2 | **미니 부하** | mini에 Qwen9B+BGE-M3 상시 + 매매엔진 + Kuma | "미니는 40잡+음성으로 압박 → 로컬티어 보류" | → **부분 수용**: 40잡이 NAS로 빠지면 미니 여유 생김. 단 24GB에 9B+임베딩+매매+voicebridge 동시는 실측 필요 |
| C3 | **벡터 저장소** | Qdrant(전용) | pgvector로 시작, 필요시 승격 | → **pgvector 시작** 권고(컨테이너 최소화·초보·2주). Qdrant는 규모 증명 후 |
| C4 | **보이스 스택** | pipecat+mlx-whisper+Kokoro 신규 이식(Studio) | 기존 voicebridge 확장(교체 아님) | → **기존 voicebridge 유지·확장**(이미 1.7GB 가동 중, 오세기 정보 기억). 레퍼 스택은 P3 개선안 |
| C5 | **NAS RAM 목표** | 문서 8→32GB / 사용자 메시지 "8→36GB" | 32GB(2×16 or 논의) | → **확인 필요**: DXP4800 공식 2슬롯(최대 64GB). 36GB는 비표준 → 32GB(2×16) 또는 40GB(8+32)? |
| C6 | **하드웨어 칩셋** | M3 Ultra / M4 단정(고정 전제) | 이전에 "미검증 추정"으로 표기 | → **사용자 확정 수용**(본인 하드웨어. Studio 83tok/s 실측이 M3 Ultra와 정합) |

## 3. 빈틈 (한쪽에만 있음 — 통합 시 흡수)

### 3-A. 내 세션에만 있음 → 사용자 문서에 흡수 필요
- **실제 사업 3개 확정**: 페이퍼크래프트(biz-a)·지역재생(regen/biz-b)·스타트업강의(edu/biz-c). 문서는 아직 "파트 대개편 예정"으로만 표기 → biz-b/c 플레이스홀더 확정.
- **24채널 재편표**(KEEP/MERGE/PRUNE) — 문서의 "40잡 이주 순서 미정"을 구체화.
- **프로덕트 개발 관리**(운영 vs 개발 분리, projects.yaml + 주간 다이제스트) — 문서에 아예 없는 영역(impact-ledger·proof-market 등).
- **도메인 게이트 재정렬**(Track A 인프라 먼저 / 도메인종속 뒤로) — 문서 G는 원래 순서(리드·블로그 1주차).
- **스킬 실측**: agent-config/skills 비어있음 + "효과 검증 후 이식"(qc-eval 로그 활용). 문서는 스킬 이식을 당연시.
- **KIS 이미 부분통합**: kis-balance 읽기전용 잔고조회 가동 중 → 매매 착수 기점.

### 3-B. 사용자 문서에만 있음 → 내 통합안에 흡수 필요
- **게이팅 3결정**(매매위치·멱등성·RTO/RPO) — 명시적 착수 전 관문. 내 세션 미다룸 → §다음 단계에서 확정.
- **라우팅 별칭 11종 + 4대 게이트**(자동승급·예산·프라이버시·시간대) — 내 "spec/codegen/review"보다 정교. 채택.
- **IP 안정화**(Tailscale MagicDNS·DHCP 예약·컨테이너명 통신·NAS 랜선 상시) — 내가 안 다룬 실운영 함정. 채택.
- **11영역 지식맵 + 성숙도 점검표** — 설계 누락 방지 프레임. 채택.
- **모델 로스터 구체**(Coder-30B 상시/Next-80B JIT/9B mini) + JIT 로드 정책 — 채택.
- **안티패턴 목록**(OpenClaw 보안사고 격리, 과분할 금지 등) — 채택.

## 4. 종합
- **골격은 완전 수렴** — 두 독립 산출물이 같은 코어에 도달(교차검증).
- **상충 6건은 대부분 "단순성 vs 정교함"** — 2주·초보 제약상 내 쪽(단순 시작)이 유리한 게 3건(C3 pgvector, C4 voicebridge 유지, C2 미니 점진), 사용자 쪽 수용 2건(C6 칩셋, 그리고 별칭·IP안정화 등 정교함은 문서 채택), C1(매매위치)·C5(RAM)는 근거 판정.
- **최대 가치는 빈틈 결합**: 내 세션의 "현실 착륙"(실제 사업·채널·프로덕트·기존자산) + 문서의 "설계 정교함"(게이팅·라우팅·IP·지식맵) = 통합본.

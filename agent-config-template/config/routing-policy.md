# 모델 라우팅 정책

> 현재 단계: **PHASE 1 — 안정화 (Claude 단일)**
> 이 파일이 곧 정책이다. 단계 전환은 이 파일을 수정·커밋하는 것으로 한다.

## PHASE 1 — 안정화 (현재)

- **모든 LLM 호출 = Claude (Anthropic API)**
- 로컬 모델은 사용하지 않는다. 목적: 변수 제거, 최소 신뢰 루프(MVL) 확립.

### PHASE 2 전환 조건 — 측정 방법까지 정의한다 (측정 불가능한 조건은 조건이 아니다)

| 조건 | 측정 방법 |
|---|---|
| 2주 연속 무단 다운 0회 | `#agent-log`의 `[hermes] 데몬 (재)시작됨` 알림(hermes.plist 래퍼가 발송) 중 운영자가 의도하지 않은 것 0건 |
| 2주 연속 미인지 실패 0회 | 사후 발견한 미알림 실패는 발견 즉시 `[postmortem]` 태그로 #agent-log에 기록한다. 판정 = 해당 기간 `[postmortem]` 건수 |
| 주간 스킬 심사 2회 수행 | `skill audit done` 기록 2건 (AUDIT.md가 0건 처리여도 기록을 의무화) |
| **관찰 시작일 기록** | 아래 변경 이력에 "PHASE 1 관찰 시작: YYYY-MM-DD" 커밋 — 이 날부터 14일 창 |

### PHASE 2 전환 전 선행 작업 (조건 충족과 별개로 완료돼야 함)

- [ ] 로컬 모델 서버 health-check 훅 작성 + heartbeat 검사 항목에 편입
- [ ] recovery.md에 Mac Studio(모델 서버) 복구 절차 추가
- [ ] observability.md 인벤토리에 Studio 항목 추가

## PHASE 2 — 하이브리드 (전환 조건 + 선행 작업 충족 후)

로컬 모델(Mac Studio, MLX 서빙 MoE)을 다음 작업에만 복귀시킨다:

| 작업 | 모델 | 이유 |
|---|---|---|
| 분류·태깅·라우팅 판단 | 로컬 (소형 MoE) | 대량·저난도, 토큰 0원 |
| 사내 문서 요약·RAG | 로컬 (대형 MoE) | 민감 데이터 외부 미전송 |
| 초안(내부용) | 로컬 | 검수 전제라 오류 허용 |
| 복잡한 다단계 판단·코딩 | Claude | 품질 격차 큼 |
| **대외 발송물 전부** | Claude + 사람 검수 | 최종 품질·책임 |
| 알림·로깅·재시작 | **모델 사용 금지 — hooks가 담당** | 결정적 작업 |

- 로컬 서버 다운 시: **감지·알림은 health-check 훅**(위 선행 작업)이 담당하고,
  **폴백 라우팅(해당 작업을 Claude로 우회)은 헤르메스 라우터**가 수행한다.
  역할 분담을 명확히 — 훅은 알리고, 라우터는 우회하고, 모델은 관제에 관여하지 않는다.

## PHASE 3 — 확장 (PHASE 2 안정 후)

- Subagent로 사업 3개 분리 (biz별 채널·스킬·권한 격리)
- 검증된 구성을 Plugin으로 패키지화 → 신규 사업 온보딩 가속

## 변경 이력

- 2026-07-20: 최초 작성. PHASE 1 시작. (관찰 시작일은 Mini 설치 완료 시 이 줄 아래에 기록할 것)
